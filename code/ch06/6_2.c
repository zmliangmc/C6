#include <stdio.h>
#include <math.h>
int main(int argc, char const *argv[])
{
    // int a = -1;
    // int b = fabs(a);
    float a = 1.0 / 3;
    float b = 1.0;
    printf("%f", a - b);
    return 0;
}
